# 我的财务台 - 维护说明书

> 最后更新：2026-09-09 | 版本：v3.2
> 本文档用于日常维护、问题排查和重新部署。

---

## 一、项目概述

「我的财务台」是一个个人财务管理 Web 应用，支持：
- 净资产看板、收支统计、图表分析
- 美股四大指数实时行情（道琼斯/标普500/纳斯达克综合/纳斯达克100）
- 纳指ETF（513100）持仓盈亏管理
- 记账流水、账户管理、负债还款
- 人情往来（生日提醒/随礼台账，支持农历）
- 车辆管理（加油/保养/车险/年检提醒）
- 财务工具（复利计算/提前还款/目标储蓄等）
- 多设备云端同步（Supabase）

---

## 二、技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 构建工具 | Vite | ^8.2.2 |
| 前端框架 | React | ^19.2.8 |
| 语言 | TypeScript | ^7.0.2 |
| 样式 | Tailwind CSS | ^4.3.3 |
| 图表 | ECharts | ^6.1.0 |
| 路由 | React Router | ^7.18.3 |
| 云端数据库 | Supabase | ^2.115.0 |
| 日期处理 | dayjs | ^1.11.23 |
| 农历换算 | lunar-javascript | ^1.7.7 |
| 图标 | lucide-react | ^1.41.0 |

---

## 三、目录结构

```
个人网页设计/
├── src/                          # 源代码
│   ├── pages/                    # 页面组件（每个文件对应一个功能页）
│   │   ├── Dashboard.tsx         # 首页看板
│   │   ├── Markets.tsx           # 行情页
│   │   ├── Holdings.tsx          # 持仓页
│   │   ├── Transactions.tsx      # 记账流水
│   │   ├── Accounts.tsx          # 账户管理
│   │   ├── Borrow.tsx            # 负债还款
│   │   ├── People.tsx            # 人情往来
│   │   ├── Cars.tsx              # 车辆管理
│   │   ├── Tools.tsx             # 财务工具
│   │   ├── Reminders.tsx         # 提醒中心
│   │   ├── Annual.tsx            # 年度统计
│   │   ├── Gains.tsx             # 收益分析
│   │   ├── Vault.tsx             # 数据保险箱
│   │   ├── More.tsx              # 更多/设置
│   │   ├── Login.tsx             # 登录页
│   │   ├── AddTransaction.tsx    # 添加交易
│   │   └── Calendar.tsx          # 日历视图
│   ├── components/               # 通用组件
│   │   ├── Layout.tsx            # 布局（侧边栏/底部导航）
│   │   ├── ui.tsx                # UI 基础组件（Card/Button/Modal等）
│   │   ├── charts.tsx            # ECharts 图表封装
│   │   └── ErrorBoundary.tsx     # 错误边界
│   ├── lib/                      # 核心逻辑
│   │   ├── store.tsx             # 全局状态管理（核心！数据增删改查都在这里）
│   │   ├── auth.tsx              # 登录认证
│   │   ├── cloud.ts              # 云端同步
│   │   ├── quotes.ts             # 行情数据获取（腾讯财经接口）
│   │   ├── market.ts             # 行情数据处理（分时/日K/走势）
│   │   ├── calc.ts               # 计算工具
│   │   ├── format.ts             # 格式化工具
│   │   ├── gains.ts              # 收益计算
│   │   ├── reminders.ts          # 提醒逻辑
│   │   ├── holidays.ts           # 节假日数据
│   │   ├── lunar.ts              # 农历工具
│   │   ├── values.ts             # 常量配置
│   │   ├── vaultCrypto.ts        # 加密工具
│   │   └── fitText.ts            # 文字适配
│   ├── demo.ts                   # 演示数据
│   ├── types.ts                  # TypeScript 类型定义
│   ├── App.tsx                   # 应用入口（路由配置）
│   ├── main.tsx                  # React 挂载入口
│   ├── index.css                 # 全局样式
│   └── vite-env.d.ts             # Vite 类型声明
├── public/                       # 静态资源（图标/manifest）
├── supabase/                     # 数据库 schema
│   └── schema.sql                # 建表 SQL
├── dist/                         # 构建产物（部署用，不要手动改）
├── .env                          # 环境变量（Supabase 配置，不要提交到公开仓库）
├── .env.example                  # 环境变量模板
├── vite.config.ts                # Vite 配置（含 base 路径）
├── tsconfig.json                 # TypeScript 配置
├── package.json                  # 依赖和脚本
├── start.bat                     # 一键启动脚本
├── 防火墙放行.bat                 # 局域网访问防火墙配置
└── 维护说明书.md                  # 本文档
```

---

## 四、快速启动

### 4.1 开发模式（本地修改代码时用）

**方式一：双击启动（推荐）**
```
双击 start.bat
```
等待出现 `Local: http://localhost:5173/` 后，用浏览器打开即可。

**方式二：命令行**
```bash
cd D:\个人文件\AI项目\个人网页设计
npm run dev
```

### 4.2 构建生产版本

```bash
cd D:\个人文件\AI项目\个人网页设计
npm run build
```
构建产物在 `dist/` 目录。

### 4.3 本地预览生产版本

```bash
npm run preview
```

---

## 五、部署说明

### 5.1 GitHub Pages（当前使用，免费永久）

**网址**：https://aman-devd.github.io/my-finance-dashboard/

**更新部署步骤**（修改代码后执行）：
```bash
cd D:\个人文件\AI项目\个人网页设计
npm run build
cd dist
git add -A
git commit -m "update: 描述修改内容"
git push -f origin master:gh-pages
```
等待 1-2 分钟后，访问网址即可看到更新（可能需要 Ctrl+F5 强制刷新）。

**注意**：`vite.config.ts` 中的 `base: '/my-finance-dashboard/'` 必须保留，否则资源路径会出错。

### 5.2 Vercel（备用，已部署）

**网址**：https://my-finance-dashboard-khaki.vercel.app
（国内访问不稳定，需要 VPN）

**更新部署**：
```bash
cd D:\个人文件\AI项目\个人网页设计
npx vercel --prod --yes
```

### 5.3 局域网访问（手机/平板同 WiFi 查看）

启动开发服务器后，终端会显示 `Network: http://192.168.x.x:5173/`，手机浏览器打开该地址即可。

如果手机打不开：
1. 双击 `防火墙放行.bat` 并点"是"
2. 确认手机和电脑连同一个 WiFi
3. 路由器可能开启了"AP 隔离"，需关闭

---

## 六、账号信息汇总

> ⚠️ 请妥善保管以下账号密码，丢失可能导致数据无法恢复。

### 6.1 GitHub（管网站代码和部署）
- 用户名：`Aman-devd`
- 仓库：https://github.com/Aman-devd/my-finance-dashboard
- 用途：存放代码、GitHub Pages 部署

### 6.2 Supabase（管你的财务数据）
- 登录邮箱：`amanboll@qq.com`
- 控制台：https://supabase.com/dashboard
- 项目 URL：`https://vrkxpggvvhdalnpsaifw.supabase.co`
- 用途：云端数据存储、多设备同步、用户认证

### 6.3 应用登录账号
- 用户名：`Amanbol`
- 用途：登录财务应用，数据与该账号绑定

### 6.4 Vercel（备用部署）
- 用户名/团队：`amanboll-7683`
- 项目：`my-finance-dashboard`
- 用途：备用部署平台

---

## 七、数据存储说明

### 7.1 数据存在哪里？

- **云端（主要）**：Supabase 数据库，登录后自动同步
- **本地（缓存）**：浏览器 localStorage，用于离线访问和加速
- **云端快照**：Supabase `user_snapshots` 表，自动保留最近 20 份历史快照

### 7.2 数据表

| 表名 | 用途 |
|------|------|
| `user_snapshots` | 用户数据快照（自动备份，保留最近20份） |

建表 SQL 在 `supabase/schema.sql`。

### 7.3 数据备份

**手动导出**：
1. 打开应用 → 「更多」页
2. 找到「历史记录」区域
3. 选择「本机」或「云端」标签
4. 点击导出，保存 JSON 文件

**自动备份**：
- 云端自动保留最近 20 份快照
- 在「更多 → 历史记录 → 云端」可查看和恢复

---

## 八、行情数据源说明

### 8.1 数据来源

所有行情数据来自**腾讯财经**免费接口：

| 数据类型 | 接口 | 刷新频率 |
|----------|------|----------|
| 实时报价 | `qt.gtimg.cn` | 15秒 |
| 日K线 | `web.ifzq.gtimg.cn/appstock/app/usfqkline` | 30分钟 |
| 分时图 | `web.ifzq.gtimg.cn/appstock/app/minute/query` | 60秒 |

### 8.2 代码映射

| 显示名称 | 接口代码 |
|----------|----------|
| 纳斯达克100 | `usNDX` |
| 纳斯达克综合 | `usIXIC` |
| 标普500 | `usINX` |
| 道琼斯 | `usDJI` |
| 纳指ETF(513100) | `sh513100` |

### 8.3 已知限制

- 免费接口**不提供**溢价率、IOPV、成交量、成交额（已从页面移除）
- 非交易时段：美股分时图显示上一交易日完整分时（本地缓存）
- A股ETF（513100）非交易时段显示收盘价水平线
- 腾讯财经接口可能偶尔超时，页面会自动重试

---

## 九、常见问题排查

### 9.1 网站打不开

**GitHub Pages 打不开**：
1. 刷新页面（Ctrl+F5 强制刷新）
2. 换手机流量试试（移动网络通常比宽带好）
3. 等几分钟再试（github.io 偶尔不稳定）
4. 确认 GitHub 仓库没有被删除

**本地开发服务器打不开**：
1. 确认 `start.bat` 正在运行（终端窗口不要关）
2. 确认端口 5173 没被占用
3. 重启：关掉终端窗口，重新双击 `start.bat`

### 9.2 登录不上 / 数据不同步

1. 确认网络连接正常
2. 确认 Supabase 服务正常（https://supabase.com/status）
3. 确认 `.env` 文件中的 Supabase URL 和 Key 正确
4. 尝试清除浏览器缓存后重新登录
5. 检查是否密码连续输错3次（会锁定1小时）

### 9.3 行情数据不更新

1. 确认网络连接正常
2. 腾讯财经接口可能临时超时，等1-2分钟自动恢复
3. 非交易时段数据不更新是正常的（美股交易时间：北京时间21:30-次日04:00）
4. 尝试刷新页面

### 9.4 图表显示异常

1. 刷新页面（Ctrl+F5）
2. 清除浏览器缓存
3. 确认 ECharts 资源加载正常（F12 看 Console 有没有报错）

### 9.5 修改代码后网站没更新

1. 确认执行了 `npm run build`
2. 确认 `dist` 目录的改动已推送到 `gh-pages` 分支
3. 等待 1-2 分钟（GitHub Pages 构建需要时间）
4. 强制刷新浏览器（Ctrl+F5）

### 9.6 npm install 失败

1. 确认网络连接正常
2. 尝试切换 npm 镜像：
   ```bash
   npm config set registry https://registry.npmmirror.com
   ```
3. 删除 `node_modules` 和 `package-lock.json` 后重新安装

### 9.7 Git 推送失败（SSL 错误）

如果出现 `CRYPT_E_NO_REVOCATION_CHECK` 错误，执行：
```bash
git config --global http.sslVerify false
git config --global http.schannelCheckRevoke false
```

---

## 十、如何修改代码

### 10.1 修改页面内容

找到 `src/pages/` 下对应的页面文件，直接修改即可。保存后开发服务器会自动热更新。

### 10.2 修改数据逻辑

所有数据增删改查都在 `src/lib/store.tsx` 中，这是最核心的文件。修改前建议先备份。

### 10.3 修改样式

- 全局样式：`src/index.css`
- 组件样式：`src/components/ui.tsx`（Card/Button/Modal 等）
- 品牌色：在 `src/index.css` 中搜索 `#0071e3`

### 10.4 添加新页面

1. 在 `src/pages/` 新建 `.tsx` 文件
2. 在 `src/App.tsx` 中添加路由
3. 在 `src/components/Layout.tsx` 中添加导航菜单项

---

## 十一、重新部署指南（电脑重装/换电脑时）

### 11.1 从 GitHub 恢复项目

```bash
# 安装 Git 和 Node.js（v18+）
# 克隆仓库
git clone https://github.com/Aman-devd/my-finance-dashboard.git
cd my-finance-dashboard

# 安装依赖
npm install

# 创建 .env 文件（参考 .env.example，填入 Supabase 配置）
# 内容：
# VITE_SUPABASE_URL=https://vrkxpggvvhdalnpsaifw.supabase.co
# VITE_SUPABASE_ANON_KEY=你的anon key

# 启动开发服务器
npm run dev
```

### 11.2 重新部署到 GitHub Pages

```bash
npm run build
cd dist
git init
git remote add origin https://github.com/Aman-devd/my-finance-dashboard.git
git add -A
git commit -m "deploy"
git push -f origin master:gh-pages
```

---

## 十二、已知限制和未来改进

### 当前限制
1. GitHub Pages 国内访问偶尔不稳定
2. 行情数据来自免费接口，部分指标（成交量等）不提供
3. 美股分时图非交易时段显示上一交易日数据（需交易时段过后才有缓存）
4. 没有原生手机 App，需要通过浏览器访问

### 可选改进方向
- 配置自定义域名（需要购买域名）
- 部署到国内云服务（腾讯云COS/阿里云OSS）提升访问速度
- 接入更专业的行情数据接口（付费）
- 开发原生手机 App

---

## 十三、文件清理记录（2026-09-09）

已删除以下不必要的文件：
- 调试脚本：`dbg.mjs`, `e2e.mjs`, `repro.mjs`, `serial.mjs`, `stress.mjs`
- 临时上传文件：`uploaded-*`, `uploaded-file`
- 临时截图：`screenshot.png`
- 构建缓存：`tsconfig.tsbuildinfo`
- 重复资料：`交接资料-20260909/`（已迁移到当前目录）
- 重复方案：`方案.md`（已合并到本文档）
- 临时说明：`快照说明.txt`

---

*本文档最后更新：2026-09-09*
*如有问题，先查阅本文档的「常见问题排查」章节。*
